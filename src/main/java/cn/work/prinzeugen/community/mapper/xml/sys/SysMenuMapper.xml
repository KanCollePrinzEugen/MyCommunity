<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.work.prinzeugen.community.mapper.sys.SysMenuMapper">
    <select id="getMenusByUserId" parameterType="Integer" resultType="cn.work.prinzeugen.community.entity.sys.SysMenu">
        SELECT * FROM super_mall.sys_menu WHERE id IN (SELECT menu_id FROM sys_menu_role WHERE role_id IN (SELECT role_id FROM sys_admin_role WHERE admin_id = ${userId}))
    </select>
</mapper>
